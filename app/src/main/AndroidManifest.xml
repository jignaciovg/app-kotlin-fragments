<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vaqueiro.apphellow">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppHelloW">
        <activity
            android:name=".activities.Fragments2Activity"
            android:exported="true" />
        <activity
            android:name=".activities.FragmentsActivity"
            android:exported="true" />
        <activity
            android:name=".activities.ToolbarActivity"
            android:exported="true" />
        <activity
            android:name=".activities.HelloActivity"
            android:exported="true" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>